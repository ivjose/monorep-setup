import React from 'react';
import { CrudProps } from '../interfaces';

const Read: React.FC<CrudProps> = ({ initialData }) => {
  console.log(initialData);

  return <div>Read DDAsdasd</div>;
};

export default Read;
